# SoyaOS Developer Portal has no public build-time variables.
# Cloudflare runtime secrets belong in `.dev.vars` locally and in Worker
# Variables and Secrets in production. Never put credentials in this file.
